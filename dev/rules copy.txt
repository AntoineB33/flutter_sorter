Widget --> holds a pointer to Controller

Controller --> holds a pointer to UseCase

UseCase --> holds a pointer to Repository Interface

Repository Impl --> holds pointers to LocalDataSource and RemoteDataSource




Functions in repositories that make updates to save locally :

  @override
  @useResult
  ChangeSet updateFunction(int sheetId, IMap<String, SyncRequest> updates) {



